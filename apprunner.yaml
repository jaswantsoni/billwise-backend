version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm install
      - npx prisma generate
    build:
      - echo "Build completed"
run:
  command: node server.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
